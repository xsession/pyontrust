"""Transport layer placeholder.

v0.1 runs acquisition in-process using a background Python thread.
This package is reserved for future streaming transports (worker threads, shared memory, or IPC).
"""
