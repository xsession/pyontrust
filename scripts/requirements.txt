
# This repository's power-test framework (`pyontrust_packages/power_test_framework`) is
# standard-library only by default.
#
# Optional tooling you may choose to install/use (NOT required by tests):
# - ffmpeg (webcam recording)
# - SEGGER J-Link tools (JLink.exe / JLinkExe)
# - Nordic tooling for PPK2 / BLE sniffer capture
# - hackrf tools (hackrf_transfer)

# CLI parsing for profile runner
docopt>=0.6.2

# NOTE: The Reflex web GUI has its own dependency set.
# Install it from `gui_app/reflex_control/requirements.txt` in a dedicated venv.
